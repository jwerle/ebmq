/* std include */
#include <stdio.h>
#include <assert.h>

/* emq libs */
//#include "../include/emq/emq.h"

int
main (int argc, char *argv[]) {
	puts("emq test");
	/* assert version which must be updated with new builds */
	//assert(EMQ_VERSION == "0.0.1");
	return 0;
}